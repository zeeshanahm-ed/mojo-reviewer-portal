export enum ROLES {
    REVIEWER = 'Reviewer',
}

export enum ROLESLABEL {
    super_admin = 'ADMIN',
    content_manager = 'CONTENT MANAGER',
    finance_manager = 'FINANCE MANAGER',
    read_only = 'READ ONLY',
    reviewer = 'REVIEWER',
    moderator = 'MODERATOR',
}